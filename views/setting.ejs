<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>設定 - 打刻アプリ</title>
    <link rel="stylesheet" href="style.css">
  </head>
  <body>
    <script>
      function copyToClipboardBegin() {
          // コピー対象をJavaScript上で変数として定義する
          var copyTargetBegin = document.getElementById("copyTargetBegin");

          // コピー対象のテキストを選択する
          copyTargetBegin.select();

          // 選択しているテキストをクリップボードにコピーする
          document.execCommand("Copy");

          // コピーをお知らせする（使わない）
          //alert("コピーできました！ : " + copyTargetBegin.value);
      }

      function copyToClipboardFinish() {
          // コピー対象をJavaScript上で変数として定義する
          var copyTargetFinish = document.getElementById("copyTargetFinish");

          // コピー対象のテキストを選択する
          copyTargetFinish.select();

          // 選択しているテキストをクリップボードにコピーする
          document.execCommand("Copy");

          // コピーをお知らせする（使わない）
          //alert("コピーできました！ : " + copyTargetFinish.value);
      }
    </script>

    <div class="header">
      <div class="header-logo">打刻アプリ</div>
      <div class="header-list">
        <ul>
          <li><a class="link" href="/">ホーム</a></li>
          <li><a class="link" href="/begin">出勤</a></li>
          <li><a class="link" href="/finish">退勤</a></li>
          <li><a class="link" href="/timestamp_list">打刻一覧</a></li>
          <li><a class="link" href="/todolist">TODOリスト編集</a></li>
          <li><a class="link" href="/setting">設定</a></li>
        </ul>
      </div>
    </div>

    <h1 class="toptitle1">設定 🛠</h1>

    <div class="normalcontainer">
      <h2 class="toptitle2">出勤・退勤時の社内チャット送信</h2>
      <p>あらかじめメッセージを設定することで、指定されたURLの社内チャットへかんたんにメッセージを送ることができます</p>
      <ul>
        <li>
           <a class="editbtn" href="/setting/edit/<%= settings[0].id %> ">編集</a> 
            <%= settings[0].name %> : <%= settings[0].content %>  
        </li>
        <li>
          <a class="editbtn" href="/setting/edit/<%= settings[1].id %> ">編集</a> 
          <%= settings[1].name %> : <%= settings[1].content %>  
        </li>
        <li>
            <a class="editbtn" href="/setting/edit/<%= settings[2].id %> ">編集</a>
           <%= settings[2].name %> : <%= settings[2].content %>   
        </li>
      </ul>
    </div>

    <div class="normalcontainer">
      <h2 class="toptitle2">退勤時の帰宅チャット送信</h2>
        <p>退勤時に家族や同居人へ帰宅の連絡ができます</p>
        <ul>
          <li>
            <a class="editbtn" href="/setting/edit/<%= settings[3].id %> ">編集</a>  
            <%= settings[3].name %> : <%= settings[3].content %> 
          </li>
          <li>
            <a class="editbtn" href="/setting/edit/<%= settings[4].id %> ">編集</a>  
            <%= settings[4].name %> : <%= settings[4].content %> 
          </li>
        </ul>
    </div>

    <div class="normalcontainer">
      <h2 class="toptitle2">出勤・退勤URLをブックマークに保存する</h2>
      <p>以下のURLをブックマークに登録することで、ブックマークからすぐに打刻することができます</p>
      <h3>出勤</h3>
      <input id="copyTargetBegin" type="text" value="http://localhost:3000/begin" readonly>
      <button onclick="copyToClipboardBegin()">コピー</button>
      <h3>退勤</h3>
      <input id="copyTargetFinish" type="text" value="http://localhost:3000/finish" readonly>
      <button onclick="copyToClipboardFinish()">コピー</button>
    </div>
  </body>
</html>