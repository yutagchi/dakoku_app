<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Hello World</title>
  </head>
  <body>
   <form action="/setting/update/<%= setting.id %>" method="post">
        <p> <%= setting.name %></p>
       <input type="text" name="newcontent" value="<%= setting.content %>">
       <input type="submit" value="更新する">
   </form>
  </body>
</html>

