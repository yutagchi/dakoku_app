<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Hello World</title>
  </head>
  <body>
    <a href="/timestamp_list_gb_days">日別に見る</a>
    <a href="/add_timestamp_edit">打刻を追加する</a>
    <ul>
        <% timestamps.forEach((timestamp) => { %>
          <li> 
            <a href="/edit/<%= timestamp.id %> ">編集</a> <a href="/confirm_delete/<%= timestamp.id %>">削除</a>
             開始: <%= timestamp.begin_time %> 終了: <%= timestamp.finish_time %> 労働時間: <%= timestamp.timediff %> 
          </li>
        <% }) %>
    </ul>
  </body>
</html>