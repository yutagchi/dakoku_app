
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Hello World</title>
  </head>
  <body>
    <ul>
        <% timestamps.forEach((timestamp) => { %>
            <li> 開始: <%= timestamp.begin_time %> 終了: <%= timestamp.finish_time %> 労働時間: <%= timestamp.timediff %> <a href="/edit/<%= timestamp.id %> ">編集</a> </li>
        <% }) %>
    </ul>
  </body>
</html>